name: monthly_expense_flutter_project
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fl_chart: ^0.66.0
  # csv: ^6.0.0
  pdf: ^3.10.0
  printing: ^5.11.0
  path_provider: ^2.1.2
  share_plus: ^10.0.0
  image_picker: ^1.0.4
  shared_preferences: ^2.5.4

  flutter_riverpod: ^2.5.1

  # Backend
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8

  # Utilities
  intl: ^0.19.0 # Reverted to standard, override handles the rest
  uuid: ^4.3.3
  google_fonts: ^6.1.0
    # Add this line
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

  # Rich Text Editor
  flutter_quill: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# ----------------------------------------------------------
# ADD THIS SECTION TO FIX THE VERSION CONFLICT
# ----------------------------------------------------------
dependency_overrides:
  intl: ^0.20.0

flutter:
  uses-material-design: true

## Add this configuration section at the bottom of the file
#flutter_launcher_icons:
#  android: "launcher_icon"
#  ios: true
#  image_path: "assets/icon/app_icon.png"
#  min_sdk_android: 21 # optional
#  # adaptive_icon_background: "#FFFFFF" # Enable this if you have a background color preference
#  # adaptive_icon_foreground: "assets/icon/app_icon.png" # For Android adaptive icons

#flutter_launcher_icons:
#  android: "launcher_icon"
#  ios: true
#  image_path: "assets/icon/app_icon.png" # Fallback for older devices/iOS
#  adaptive_icon_background: "#FFFFFF" # Or your brand color hex code
#  adaptive_icon_foreground: "assets/icon/foreground.png"


flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  # Remove the adaptive_icon lines for now to ensure it works first

# Add this at the very bottom of pubspec.yaml

flutter_native_splash:
  color: "#ffffff" # Background color (White)
  image: "assets/icon/app_icon.png" # The logo to show in the center
  branding: "assets/icon/app_icon.png" # Keeps logo on older Android versions
  color_dark: "#121212" # Background for Dark Mode users
  image_dark: "assets/icon/app_icon.png" # Logo for Dark Mode

  android_12:
    image: "assets/icon/app_icon.png"
    icon_background_color: "#ffffff"
    image_dark: "assets/icon/app_icon.png"
    icon_background_color_dark: "#121212"